<%= form_for([@user, @identity]) do |f| %>
  <% if @identity.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@identity.errors.count, "error") %> prohibited this identity from being saved:</h2>

      <ul>
      <% @identity.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :identity_type %><br />
    <%= f.text_field :identity_type %>
  </div>
  <div class="field">
    <%= f.label :identity_value %><br />
    <%= f.text_field :identity_value %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
